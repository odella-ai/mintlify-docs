---
title: "Data Types in Lawme"
description: "A comprehensive guide to data types used in Lawme workflows"
icon: "database"
---

import { Callout } from '@mintlify/components';

![Block Menu](/snippets/block_menu.gif)

# Understanding Data Types in Lawme

When working with Lawme workflows, it's crucial to understand the various data types used internally. This knowledge is especially important when integrating Lawme with your own applications.

## DataValue

A DataValue is the fundamental unit of data in Lawme. It's represented as an object with two key properties:

- `type`: Indicates the data type (see the Types table below)
- `value`: Contains the actual data

<Callout type="info">
  The `type` property may include additional decorators, which modify how the type behaves.
</Callout>

## Type Decorators

Decorators can be applied to types to modify their behavior. They can be combined, for example: `fn<string[]>`.

| Decorator  | Description                                                   |
|------------|---------------------------------------------------------------|
| `[]`       | Indicates an array of the specified type                      |
| `fn<type>` | Represents a function that returns the specified type         |

## Lawme Data Types

Here's a comprehensive list of data types used in Lawme:

| Type                    | Description                                       | TypeScript Equivalent                                         | Notes                                                                                                    |
|-------------------------|---------------------------------------------------|---------------------------------------------------------------|----------------------------------------------------------------------------------------------------------|
| `any`                   | Can store any type of data                        | `unknown`                                                     | Use sparingly; Lawme attempts to infer or coerce to a specific type when possible                       |
| `boolean`               | True or false value                               | `boolean`                                                     | Equivalent to JavaScript boolean                                                                         |
| `string`                | Text data                                         | `string`                                                      | Equivalent to JavaScript string                                                                          |
| `number`                | Numeric value                                     | `number`                                                      | Equivalent to JavaScript number                                                                          |
| `date`                  | Date value                                        | `string`                                                      | Stored as ISO-8601 date string                                                                           |
| `time`                  | Time value                                        | `string`                                                      | Stored as ISO-8601 time string                                                                           |
| `datetime`              | Date and time value                               | `string`                                                      | Stored as ISO-8601 datetime string                                                                       |
| `chat-message`          | Representation of a chat message for LLMs         | `{ type: string; message: string; name?: string; function_call?: string }` | Includes metadata such as sender information                                                             |
| `object`                | Complex data structure                            | `Record<string, unknown>`                                     | Similar to JavaScript object; may sometimes be used interchangeably with `any` or represent an array    |
| `control-flow-excluded` | Value excluded from control flow                  | `undefined`                                                   | Used in specific control flow scenarios                                                                  |
| `vector`                | Numeric vector                                    | `number[]`                                                    | Commonly used for embeddings in machine learning contexts                                                |
| `image`                 | Image data                                        | `{ mediaType: string; data: UInt8Array; }`                    | Includes media type information                                                                          |
| `audio`                 | Audio data                                        | `{ mediaType: string; data: UInt8Array; }`                    | Includes media type information                                                                          |
| `binary`                | Raw binary data                                   | `UInt8Array`                                                  | Used for handling binary file contents or other raw data                                                 |

<Callout type="warning">
  When integrating with Lawme, ensure you handle these data types correctly to maintain data integrity and workflow functionality.
</Callout>
